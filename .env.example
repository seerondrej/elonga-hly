# Database connection (required for backend)
DB_HOST=your-db-host.com
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name
DB_PORT=3306

# API port (Railway sets PORT automatically)
PORT=3001
